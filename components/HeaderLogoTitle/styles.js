import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  coinHeader: {
    flexDirection: 'row',
    alignItems: 'center',
  },

  logo: {
    marginRight: 8,
  },
});

export default styles;
