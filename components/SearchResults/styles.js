import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    marginVertical: 8,
  },

  title: {
    marginLeft: 16,
    marginBottom: 8,
  },
});

export default styles;
