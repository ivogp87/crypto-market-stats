import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  exchangeInfo: {
    marginTop: 16,
    marginBottom: 32,
  },

  marginTop: {
    marginTop: 16,
  },

  description: {
    margin: 16,
  },
});

export default styles;
