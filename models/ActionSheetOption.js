class ActionSheetOption {
  constructor(option, value, buttonTitle, buttonIcon) {
    this.option = option;
    this.value = value;
    this.buttonTitle = buttonTitle;
    this.buttonIcon = buttonIcon;
  }
}

export default ActionSheetOption;
