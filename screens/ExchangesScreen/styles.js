import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    paddingVertical: 16,
  },

  loadMoreButton: {
    marginTop: 8,
    marginHorizontal: 16,
  },
});

export default styles;
