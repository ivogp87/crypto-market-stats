import { StyleSheet } from 'react-native';

const sharedStyles = StyleSheet.create({
  screenContainer: {
    flex: 1,
    paddingVertical: 16,
  },

  loadMoreButton: {
    marginTop: 8,
    marginHorizontal: 16,
  },

  flexOne: {
    flex: 1,
  },
});

export default sharedStyles;
